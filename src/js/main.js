/**
 * STECH theme — JS entry.
 * Bundled to public/js/app.min.js via esbuild.
 *
 * blocks.js holds the approved, self-initialising block behaviours
 * (nav toggle, accordion, tabs, carousels, stat count-up, marquee, dismiss…),
 * all of which honour prefers-reduced-motion. Theme-level partials
 * (header scroll state, skip-link focus) live in ./partials/.
 */
import './blocks.js';
import './partials/header.js';
